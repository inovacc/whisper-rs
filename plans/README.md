# Implementation Plans

Generated by the `improve` skill on 2026-07-14 (planned at commit `1cfc289`). Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done. Plans were vetted against the live code by the advisor.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix streaming finalize/commit + add `Transcribe` seam | P1 | M | — | DONE (54c7c4c) |
| 002 | Reconcile README/AGENTS/ISSUES with shipped code | P1 | S | — | DONE |
| 003 | Delete dead code (`ndarray` dep, `Context::as_ptr`) | P2 | S | — | DONE |
| 004 | Fix VAD `min_speech_ms` filter (inert at defaults) | P2 | S | — | TODO |
| 005 | Harden the model downloader (traversal, checksum, truncation) | P1 | M | — | TODO |
| 006 | Speed up agglomerative clustering (O(n³)→O(n²)) | P2 | M | — | TODO |
| 007 | Flush resampler delay so timestamps stay aligned | P2 | M | — | TODO |
| 008 | Add CI gates: fmt check, cargo-audit, MSRV leg | P2 | S | — | TODO |
| 009 | Close test-coverage gaps (float WAV, downloader, cfg branch) | P3 | S | 001, 005 | TODO |
| 010 | Cleanups (in-place preprocess, token-join, cache dir, doc note) | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes
- **009** depends on **001** (the `Transcribe` seam lets `StreamSession` be unit-tested offline) and on
  **005** (the downloader-id validation is what 009's traversal test asserts).
- **008** should run **last** among code-changing plans: adding `rustfmt.toml` + running `cargo fmt` once
  will reflow lines; doing it after 001–007/010 avoids re-formatting churn on top of their diffs.
- 001–007 and 010 are otherwise independent and may be done in any order / parallel worktrees.

## Findings considered and rejected (so they aren't re-audited)
- **Hallucination flags no-overlap segments** (`postprocess/hallucination.rs:27-32`): *by design* — a
  primary segment the second pass never produced is legitimately suspicious; it is the tested behavior.
  Only a doc note is warranted (folded into plan 010), not a logic change.
- **8-bit unsigned WAV decode** (`audio/mod.rs:25-31`): LOW confidence — depends on `hound`'s presentation
  of 8-bit samples; 8-bit WAV is rare. Not planned; revisit only if an 8-bit input bug is reported.
- **Default feature set pulls ureq+ndarray** (`Cargo.toml:10`): design judgment with real downstream-API
  impact (changing `default` breaks consumers relying on it). Left for the maintainer to decide; not a bug.
  (Note: plan 003 removes the *dead* `ndarray`, which is unambiguous.)
- **`Token` vs `Word` near-duplication**, **indexing `assign_speakers`/`flag_hallucinations`**: below the
  rule-of-three / no concrete cost — rejected per the restraint lens.
